# Development: the repository code replaces the one in the image.
# To test the published image as is: docker compose -f compose.yaml up -d
services:
  wordpress:
    volumes:
      - wp_data:/var/www/html
      - ./:/var/www/html/wp-content/plugins/geofolio
